{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "bash -c 'alembic upgrade head && python main.py'",
    "restartPolicyType": "ON_FAILURE"
  }
}
